# Preferred local setup: keep the key in a local file and point to it.
GOOGLE_APPLICATION_CREDENTIALS_FILE=
GOOGLE_CLOUD_PROJECT=rethread-tag-ingestion
# Optional alternative: inline service account JSON (do not commit real values).
GOOGLE_APPLICATION_CREDENTIALS_JSON=
GEMINI_API_KEY=
GOOGLE_MAPS_API_KEY=
DOCAI_PROCESSOR_ID=
BIGQUERY_DATASET=rethread-tag-ingestion.brand_ratings
